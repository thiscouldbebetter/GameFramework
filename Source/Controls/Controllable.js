

class Controllable
{
	toControl;

	constructor(toControl)
	{
		this.toControl = toControl;
	}

	// EntityProperty.

	finalize(u, w, p, e){}
	initialize(u, w, p, e){}
	updateForTimerTick(u, w, p, e){}
}
