

class Traversable
{
	isBlocking

	constructor(isBlocking)
	{
		this.isBlocking = isBlocking;
	}

	// EntityProperty.

	finalize(u, w, p, e){}
	initialize(u, w, p, e){}
	updateForTimerTick(u, w, p, e){}
}
